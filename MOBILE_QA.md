# MOBILE_QA.md — 출시 전 모바일 QA 점검

> 2026-04-30 작성. 모바일 QA 담당자 시점 전수조사.
> 카테고리·심각도·재현 시나리오·수정 방향까지 actionable하게 정리.

---

## 🔴 Critical — 출시 전 반드시 수정

### Q-001 · 오프라인 처리 완전 부재
**증상**: 비행기 모드·지하철·엘리베이터 등 네트워크 끊김 시 → 무한 로딩 / 빈 화면 / 알 수 없는 에러
**근거**: `navigator.onLine` 검사 0건. Service Worker 없음.
**재현**: 앱 켜기 → 비행기 모드 → 체크리스트 추가 시도 → 무반응
**영향**: 모바일 앱 표준 미달. 출시 후 1성 리뷰 다발 예상.
**수정 방향**:
- 네트워크 상태 hook 추가 (`navigator.onLine` + `online`/`offline` 이벤트)
- 오프라인 시 상단 배너 ("오프라인 — 변경사항이 저장되지 않습니다")
- 폼 submit 시 오프라인이면 즉시 로컬 알림 + 재시도 큐
- 작업량: 2~3시간

---

### Q-002 · 이미지 EXIF 위치 정보 미제거
**증상**: 하객이 식장에서 QR로 사진 업로드 시 GPS 좌표가 EXIF에 그대로 저장
**근거**: [app/api/guest/upload/route.js](app/api/guest/upload/route.js) 검증만 하고 EXIF 처리 없음. `piexif`·`exifr` 등 라이브러리 미사용.
**재현**: GPS 켠 폰으로 사진 찍고 업로드 → DB에 저장된 파일 EXIF 확인 시 위도/경도 노출
**영향**: 개인정보 보호법 위반 소지. 식장 위치 + 하객 식별 정보가 결합되면 사생활 침해.
**수정 방향**:
- 클라이언트 업로드 전 EXIF strip (sharp WASM 또는 Canvas 재인코딩)
- 또는 서버 처리 (sharp + Vercel function)
- 작업량: 2~3시간

---

### Q-003 · 이미지 클라이언트 압축·리사이즈 없음
**증상**: 사용자가 12MP 폰 카메라 풀해상도(8MB+) 사진 그대로 업로드 → 모바일 데이터 낭비, 업로드 5초+
**근거**: 현재 10MB 미만 통과시키는데, 클라이언트 압축 없음. 이미지 라이브러리 미설치.
**재현**: 5G 끄고 LTE에서 청첩장 사진 5장 추가 → 30초 이상 대기
**영향**: 사용자 이탈, 통신비 부담. 식장에서 하객들이 4G로 업로드 시 식장 와이파이 마비 가능.
**수정 방향**:
- `browser-image-compression` 또는 Canvas로 1600px 폭/85% 품질 리사이즈
- 적용 위치: invitation/photo, invitation/cover, guest/upload
- 작업량: 2시간 (Q-002와 같은 라이브러리로 같이 처리 가능)

---

### Q-004 · iOS 인풋 자동 줌 (16px 미만 폰트)
**증상**: iOS Safari/WebView에서 input·textarea의 font-size가 16px 미만이면 포커스 시 화면 자동 확대 → 기괴한 UX
**근거**: [app/globals.css](app/globals.css)의 `.input-field` 글자 크기 명시 없음. 부모 폰트(Pretendard, 기본 16px)로 fallback되지만 모든 인풋이 그 사이즈는 아님.
**재현**: iPhone Safari → /timeline 빠른 추가 인풋 클릭 → 화면 확대됨
**영향**: 출시 후 iOS 사용자 즉시 발견.
**수정 방향**:
```css
.input-field, input, textarea, select { font-size: 16px; }
```
- 작업량: 10분

---

## 🟠 High — 모바일 표준 누락

### Q-005 · Pull-to-refresh 없음
**증상**: 모바일 앱 표준 인터랙션. 화면 위에서 끌어내리면 새로고침. 부재 시 사용자가 "갱신 안 됨"이라 오해.
**근거**: 검색 결과 0건.
**수정 방향**: Capacitor + 모바일 webview에서 작동하는 `react-pull-to-refresh` 또는 자체 구현. dashboard/timeline/guests에 우선 적용. 1~2시간.

---

### Q-006 · Capacitor 핵심 플러그인 3종 미설치
**누락 플러그인**:
- **`@capacitor/keyboard`** — 키보드가 입력창 가릴 때 자동 스크롤/패딩
- **`@capacitor/status-bar`** — Ink(#1A1613) 배경에 맞는 상태바 색상
- **`@capacitor/haptics`** — 체크/저장 시 가벼운 진동 피드백

**근거**: package.json에 `app/browser/clipboard`만 있음.
**재현 1**: 메모 입력창 클릭 → 키보드 올라옴 → 입력창이 키보드 뒤에 가려져 안 보임
**재현 2**: 상태바가 흰색이고 앱 헤더와 부조화
**재현 3**: 체크박스 눌러도 무감각

**수정 방향**:
```bash
npm i @capacitor/keyboard @capacitor/status-bar @capacitor/haptics
npx cap sync android
```
초기화는 `app/layout.js`나 별도 `NativeBridge.js`에서.
작업량: 1~2시간 (플러그인 + 통합 + 테스트)

---

### Q-007 · Realtime 동시편집 last-write-wins
**증상**: 신랑·신부가 같은 의사결정 항목을 동시에 편집 → 늦게 저장한 쪽이 상대방 변경을 덮어씀
**근거**: `decisions`·`couple_notes` 등에서 낙관적 락 / 충돌 감지 없음.
**재현**: 신랑 폰 + 신부 폰 동시 열어서 같은 결정의 의견을 각자 편집·저장 → 한쪽 사라짐
**영향**: 데이터 손실. 결혼 같이 준비하는 앱에서 큰 신뢰 이슈.
**수정 방향**:
- 단순 처방: `updated_at` 비교로 "다른 사용자가 먼저 변경했어요. 새로고침 후 다시 시도" 토스트
- 정밀 처방: CRDT (yjs) — 비용 큼, 출시 후 평가
- 작업량: 단순처방 1시간

---

### Q-008 · maxLength 보호 부족 (9곳만)
**근거**: 인풋 maxLength 9건만 사용. 메모·청첩장 본문 등에 미적용.
**재현**: 청첩장 message에 100,000자 붙여넣기 → DB 입력 시도 → Supabase 에러
**수정 방향**:
- 모든 textarea·input에 합리적 maxLength + 글자수 표시 ("234 / 500")
- 작업량: 1시간

---

## 🟡 Medium — 사용자 일부에서 발생

### Q-009 · 이미지 lazy loading 1곳만
**근거**: `loading="lazy"` 1건. invitation/gallery에서 첫 진입 시 모든 사진 동시 로드.
**수정**: `<img loading="lazy">` 일괄 적용. 30분.

---

### Q-010 · 이미지 onError fallback 2곳만
**근거**: 사진 URL 깨졌을 때 빈 박스만 표시. notes 페이지에만 적용.
**수정**: `<img onError={...}>` 패턴 EmptyImage 컴포넌트로 일괄 교체. 1시간.

---

### Q-011 · timezone 가정 (KST 고정)
**근거**: `Intl.DateTimeFormat` 미사용. `new Date()` 직접 비교.
**재현**: 시간대 미국으로 변경된 폰에서 D-day가 1일 다르게 표시될 수 있음
**영향**: 한국 사용자 100% 가정이면 OK. 글로벌 진출 시 필수.
**우선순위**: 출시 후 평가.

---

### Q-012 · 시스템 폰트 스케일 미대응
**근거**: `rem` 단위 2건만 사용. 대부분 `text-sm` (14px), `style={{fontSize: 13}}` 등 픽셀 고정.
**재현**: Android 설정 → 글자 크게(가장 큼) → 카드 안 텍스트 짤림 가능
**영향**: 시각 약자·고연령층 접근성. Play Store 접근성 점수 영향.
**수정 방향**: 본문 텍스트 점진적으로 rem 전환 (출시 후 단계적 OK).

---

### Q-013 · 세션 토큰 갱신 처리
**근거**: [lib/useCouple.js](lib/useCouple.js)에서 `SIGNED_OUT`만 처리. `TOKEN_REFRESHED`는 Supabase가 자동 처리하지만 명시적 가드 없음.
**재현 1**: 앱 백그라운드에서 1시간 → 포어그라운드 복귀 → 첫 쿼리 401 → useCouple이 redirect
**수정 방향**: `TOKEN_REFRESHED` 이벤트에서 데이터 재페치 / `App.addListener('appStateChange')` 활용. 30분.

---

### Q-014 · 하객 RSVP 중복 응답 UI 약함
**근거**: 서버에서 중복 검증하지만 사용자 메시지 톤 단조로움.
**재현**: 같은 이름으로 RSVP 두 번 → "이미 응답하셨습니다" 정도
**수정 방향**: 첫 응답 내용을 보여주고 "수정하시겠습니까?" 명확히. 30분.

---

## 🟢 권장 (출시 후 단계적)

### Q-015 · Haptics 진동 피드백
체크 완료, 저장 성공, 에러 시 가벼운 진동. Q-006에서 같이 처리.

### Q-016 · 다크모드
결혼앱은 light 톤이 자연스러우므로 우선순위 낮음. 출시 후 사용자 요청 보고 결정.

### Q-017 · Splash screen 검증
[capacitor.config.ts](capacitor.config.ts)에 1500ms 설정됨. 실기기에서 깜빡임/전환 자연스러운지 확인 필요.

---

## 📋 출시 전 실기기 테스트 체크리스트

### 디바이스 권장 (최소)
- Android 14 (API 34) — 최신
- Android 11 — 최저 지원
- iPhone iOS 17 — 추후 iOS 출시 시
- 가능하면 갤럭시 + 픽셀 + 샤오미 (제조사별 webview 차이)

### 기능별 시나리오
- [ ] 회원가입 (이메일) → 커플 연동 → 결혼정보 입력
- [ ] OAuth 로그인 (Google + 카카오)
- [ ] 비밀번호 재설정 (이메일 받기 → 링크 → 재설정)
- [ ] 체크리스트 추가/완료/필터 (빠른 추가 Enter)
- [ ] 의사결정 3-way 비교 (별점, 비교표, "이걸로 결정")
- [ ] 예산 — 결제 캘린더, 세부 라인, 영수증 첨부
- [ ] 하객 — 이름·연락처·축의금 입력, 정렬·필터
- [ ] 청첩장 편집 → 카카오톡 공유 → 외부 브라우저에서 열기
- [ ] 청첩장 RSVP 응답 → 신랑 폰에 실시간 반영
- [ ] 방명록 작성 → 양쪽 폰에서 동시 확인
- [ ] QR 사진 업로드 (외부 폰으로 게스트 페이지 접속)
- [ ] 라이브 슬라이드쇼 (`/live/[code]`) 풀스크린 동작
- [ ] 회원 탈퇴 → 모든 데이터 삭제 확인

### 환경 시나리오
- [ ] 비행기 모드 → 정상 사용 → 비행기 모드 해제 → 데이터 sync
- [ ] 지하철 약한 5G → 페이지 전환 멈춤 없는지
- [ ] 백그라운드 5분 → 포어그라운드 복귀 → 토큰 만료 처리
- [ ] 화면 회전 (가로) → 레이아웃 안 깨짐
- [ ] 시스템 글자 크게 → 텍스트 짤림 없음
- [ ] 다크모드 시스템 → 앱은 라이트 유지 (현재 의도)
- [ ] 키보드 IME 전환 (한/영) → 인풋 안 가려짐 (Q-006 수정 후)
- [ ] 카카오톡으로 청첩장 링크 받음 → 앱 자동 오픈 (App Link, 키스토어 후)

### 성능 시나리오
- [ ] 청첩장 사진 5장 일괄 추가 → 응답 시간
- [ ] 하객 200명 등록 → 스크롤 성능
- [ ] 의사결정 항목 50개 + 후보 3개씩 → 비교표 모달 렌더링
- [ ] 체크리스트 200개 + 1000자 메모 → 입력 지연

---

## 🎯 우선순위 권고

**출시 전 차단**: Q-001(오프라인) · Q-002(EXIF) · Q-004(iOS 줌)
**출시 전 권장**: Q-003(이미지 압축) · Q-006(Capacitor 플러그인 3종) · Q-007(동시편집)
**출시 후 단계적**: 나머지 11개

→ Q-001/002/003/006은 한 사이클로 묶어 처리 가능 (이미지 처리 라이브러리 + Capacitor 플러그인 + 네트워크 hook). 4~6시간 작업.
