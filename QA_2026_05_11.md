# QA_2026_05_11 — 출시 전 라이브 E2E 검증 결과

> 실행일: 2026-06-01 · 라이브 Supabase (prod env) 직접 쿼리 + 코드 경로 추적
> 스크립트: `scripts/qa-verify-2026-05-11.mjs`, `scripts/qa-probe-policies.mjs`, `scripts/qa-probe-authed.mjs`

## 요약

- **PASS**: 12 / 14 항목
- **WARN**: 2 항목 (RLS 정책 문서-실제 불일치 — 사용자 영향 없음)
- **FAIL**: 0 항목

---

## 검증 매트릭스

| # | 항목 | 상태 | 비고 |
|---|------|------|------|
| 1 | reports 테이블 + RLS | WARN | SR select=OK, anon select=차단(OK). **anon INSERT 실패** — `with check (true)` 정책이 실제 DB에 없음(42501). 그러나 `/api/report`가 service role로 인서트하므로 앱 동작 정상. |
| 2 | couples count (랜딩 stats) | PASS | SR=7, anon=0 — `/api/stats`가 service role 사용 정상. |
| 3 | invitations RLS (anon 컬럼 차단) | PASS | safe 컬럼 SELECT 성공, `account_*` 차단(permission denied), slug=null 행 0개. |
| 4 | invitation_guestbook RLS | WARN | anon SELECT=차단(OK), 인증 SELECT=성공(OK). **anon INSERT 실패**("permission denied for table") — `revoke select`와 함께 INSERT GRANT까지 떨어진 듯. `/api/guestbook` POST가 service role로 우회하므로 공개 청첩장 폼 동작 정상. |
| 5 | couple_notes.image_url | PASS | 컬럼 존재, 기존 5개 행 모두 null 정상. |
| 6 | Storage 버킷 | PASS | note-images(public, jpeg/png/webp/heic/heif, 15MB) · guest-photos(private) · invitation-covers(public, 10MB). |
| 7 | rsvp_responses.message DROP | PASS | 컬럼 부재 확인. |
| 8 | /api/report 안전성 | PASS | `isOneOf(target_type)` + `isUUID(target_id)` + `sanitizeString(reason, 5~500)` 검증; DB 에러는 일반 500 메시지로 마스킹; rate limit 5/15분; 한글 메시지 응답. |
| 9 | /api/delete-account partner-aware | PASS | 코드: 파트너 있으면 `couple_id=null`로 분리, 없으면 couples 전체 삭제. 라이브 DB에 2명짜리 커플 2개 존재해 분기 동작 보호됨. |
| 10 | AndroidManifest deep-link | PASS | `/i/`, `/auth/`, `/rsvp/`, `/guest/`, `/live/` 모두 존재. `<queries>` VIEW intent 블록 존재. `screenOrientation="portrait"` + `resizeableActivity="false"` 확인. autoVerify=true. |
| 11 | App URL Open listener | PASS | `components/BackButtonHandler.js`의 `App.addListener('appUrlOpen', ...)`가 pathname+search+hash로 WebView 이동. |
| 12 | assetlinks.json (debug 지문) | PASS | SHA-256 `D6:4A:...:C7` 확인. **TODO**: 프로덕션 키스토어 지문 추가 필요 (MANUAL_TASKS MT-001). |
| 13 | /account-deletion 비인증 접근 | PASS | `middleware.js` PROTECTED_ROUTES에 없음 — 공개 접근 가능. 페이지에 삭제 절차·이메일 안내 모두 포함. |
| 14 | Sentry 설정 (DSN 미설정 시) | PASS | `sentry.server.config.js` + `sentry.edge.config.js` 모두 `enabled: !!DSN && process.env.NODE_ENV === 'production'` — DSN 없으면 init이 throw하지 않음. |

---

## WARN 상세 — 문서/실제 RLS 불일치

### W1. `reports` 테이블 — `for insert with check (true)` 정책 누락

**증상**: anon POST `/rest/v1/reports` → `42501 new row violates row-level security policy`
**기대**: SUPABASE.md L472 / MANUAL_TASKS MT-018 의 SQL대로면 통과해야 함
**원인 추정**: MT-018 SQL을 실행할 때 `create policy "anyone can report"` 구문이 누락되거나 실패한 채 RLS만 활성화됨
**사용자 영향**: **없음** — `/api/report` 라우트가 `SUPABASE_SERVICE_ROLE_KEY`로 인서트하므로 신고 기능 정상 동작
**조치**: Supabase Dashboard → SQL Editor에서 아래 한 줄만 실행하면 문서·코드 일치:
```sql
create policy "anyone can report" on public.reports
  for insert with check (true);
```

### W2. `invitation_guestbook` — anon INSERT GRANT 누락

**증상**: anon POST → `permission denied for table invitation_guestbook` (테이블 수준 GRANT 부재)
**기대**: SUPABASE.md L437 `for insert with check (true)` + 묵시적 INSERT GRANT
**원인 추정**: `revoke select on invitation_guestbook from anon` 실행 시 다른 권한도 같이 떨어졌거나, anon에 INSERT GRANT가 명시 부여되지 않음
**사용자 영향**: **없음** — 공개 청첩장 폼(`/i/[slug]`)이 `/api/guestbook` POST(service role)를 통해 인서트. 인증 SELECT(대시보드·invitation 페이지)도 정상.
**조치** (선택):
```sql
grant insert on public.invitation_guestbook to anon;
```
(실제로는 service role 우회 패턴을 정식 채택하고 SUPABASE.md L437 정책 라인을 정확히 반영하도록 문서만 정리해도 됨.)

---

## TODO / 후속 조치 (이번 검증 범위 밖)

- [ ] **MT-001 prod 키스토어 SHA-256** — assetlinks.json에 production 지문 추가 (사용자 작업)
- [ ] **카카오톡 공유 실키** — 임시 JS 키 → 실 키 교체 (수동)
- [ ] 위 W1/W2 둘 중 하나 선택: (a) DB에 누락된 정책/GRANT 실행, (b) SUPABASE.md에서 "anon 직접 INSERT 허용" 문구를 "service-role API 경유" 패턴으로 수정

---

## 검증 도구

- `scripts/qa-verify-2026-05-11.mjs` — 메인 검증 (테이블/RLS/스토리지/컬럼)
- `scripts/qa-probe-policies.mjs` — 원시 REST 호출로 anon INSERT 거부 사유 분리(42501 vs permission denied)
- `scripts/qa-probe-authed.mjs` — 인증 사용자 SELECT 정상 동작 확인

모두 `node --env-file=.env.local scripts/<file>.mjs` 로 재현 가능. `.env.local`은 worktree에 커밋되지 않음.
